setuptools>=84.0.0
django>=5.2.17,<6.0
djangorestframework>=3.18.0
psycopg[binary]>=3.1
dj-database-url==2.2
python-decouple==3.8
djangorestframework-simplejwt>=5.5.1
django-cors-headers==4.4
whitenoise==6.7
celery==5.4
redis==5.0
channels==4.1
channels-redis==4.2
daphne>=4.2.3
anthropic
twilio==9.3
supabase
Pillow
python-magic
django-celery-beat>=2.9.0
requests
gunicorn==22.0.0
pytz

groq
django-ratelimit==4.1.0

# Transitive via daphne->autobahn. Pinned to 5.9.0, which ships a pure-Python
# (py3-none-any) wheel: it needs no Rust toolchain AND carries the fixes for
# PYSEC-2024-155 / 2025-90 / 2025-238 / 2026-2123. The previous `cbor2<5.5`
# workaround avoided a build failure but held back those four CVEs.
cbor2==5.9.0
